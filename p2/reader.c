#include <stdio.h>
#include <stdlib.h>


int(){
    int MAX_BUFFER = 50960
    char *buffer = malloc(MAX_BUFFER * sizeof(char))

    while(fgets(buffer, MAX_BUFFER, stdin) != NULL){
        if(){
            fprintf(stderr, "Exceed the max length")
        }

    }
}